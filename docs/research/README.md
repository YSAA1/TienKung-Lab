# 研究笔记（非权威）

本目录是论文摘录、一次性对照和失败诊断，**不能**当开训、接票或合同真值。

现行入口：`docs/README.md`。  
现行执行：`docs/plans/` 里仅有的 active 计划。  
旧笔记里的 `PROJECT_CONTEXT.md` / `HANDOFF.md` 已撤；以 `docs/README.md` 和 `.harness/` 为准。

读这里之前先确认对应结论是否已经写进 Spec / 计划 / `schemas.py`。
