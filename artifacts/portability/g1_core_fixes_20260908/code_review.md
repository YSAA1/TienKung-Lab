# 核心修复独立 code-review

固定基线：`c2cb0440519cb6a35b54c5554da2a6774398d577`。
审核实现：`24bf0aa22f3e53389b2abb7c6db0950bbbe927f8`。
命令：`git diff c2cb0440519cb6a35b54c5554da2a6774398d577...24bf0aa22f3e53389b2abb7c6db0950bbbe927f8`。
两个独立审核代理分别只读检查仓库规范和修复计划，没有参与实现。

## Standards

无阻断发现。硬规则0项，主观建议0项。

生产代码符合AGENTS.md的小范围修改与共享算法边界要求；G1名称仅用于训练与验收配方。std约束集中在策略模块，AMP插值集中到统一批处理实现。旧加载接口的转发用于兼容，不构成需要本轮删除的中间层。验收明确区分接口验证与运动能力。

## Spec

无阻断代码发现，未发现未经要求的算法变化。

F1–F6/R1符合计划：PhysX足速度播种保留非零初速；timeout逐步替换且真实失败优先；终点Critic使用临时历史；std在初始化、加载、更新后落实；heading门控及专家时间轴正确。深度蒸馏保持原接口，未支持的AMP终点接口显式拒绝。

实际JSON确认G1零速reset首步EMA为0，第二步真实加速度继续生效；双卡两次更新后policy、discriminator、AMP normalizer差异为0，std约0.050201，checkpoint加载一致。

补充R2审核已逐项核验7个生产文件、7份验收JSON与manifest中的SHA；固定IsaacLab提交、唯一assets.py补丁、实际版本/导入路径、URDF与17专家来源完整。无剩余开训阻断问题，可在完成归档提交后按已有授权启动。

Standards：0项、无最高严重问题；Spec：0项、无最高严重问题。后续文档、数据归档及验收脚本格式整理没有改变已审核的7个生产文件。
